'use client';

import React from 'react';
import DashboardPage from '../../dashboard/page';

// Temporarily wrapping the existing dashboard while migration happens
export default function ScholarDashboard() {
  return <DashboardPage />;
}
